@model IEnumerable<Project_college.Models.Enseigne>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<span class="badge badge-primary">Enseigne</span>


    <table class="table">
        <thead>
            <tr class="table-primary">
                <th>
                    @Html.DisplayNameFor(model => model.Classe)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Enseignant)
                </th>
                <th><a asp-action="Create" class="btn btn-outline-success"><i class="far fa-plus-square"></i> Enseigne</a></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr class="table-active">
                    <td>
                        @Html.DisplayFor(modelItem => item.Classe.ClasseId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Enseignant.cin)
                    </td>
                    <td>
                        <a asp-action="Create" asp-route-id="@item.EnseigneId"><i class="fa fa-marker fa-lg"></i></a>
                        <a asp-action="Delete" asp-route-id="@item.EnseigneId" class="text-danger ml-1" onclick="return confirm('Are you sure to delete this record?')"><i class="fa fa-trash-alt fa-lg"></i></a>
                </tr>
            }
        </tbody>
    </table>
