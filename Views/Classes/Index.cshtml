@model IEnumerable<Project_college.Models.Classe>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<span class="badge badge-primary">Classe</span>


    <table class="table">
        <thead>
            <tr class="table-primary">
                <th>
                    @Html.DisplayNameFor(model => model.niveau)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.nbrEtudiant)
                </th>
                <th><a asp-action="Create" class="btn btn-outline-success"><i class="far fa-plus-square"></i> Classe</a></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr class="table-active">
                    <td>
                        @Html.DisplayFor(modelItem => item.niveau)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.nbrEtudiant)
                    </td>
                    <td>
                        <a asp-action="Create" asp-route-id="@item.ClasseId"><i class="fa fa-marker fa-lg"></i></a>
                        <a asp-action="Delete" asp-route-id="@item.ClasseId" class="text-danger ml-1" onclick="return confirm('Are you sure to delete this record?')"><i class="fa fa-trash-alt fa-lg"></i></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
